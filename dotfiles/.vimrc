
" enable syntax highlighting
syntax on
set background=dark
colorscheme solarized

" turn on the mouse if we have one
if has('mouse')
   set mouse=a
endif

" Tabs to spaces
set smartindent
set tabstop=4
set shiftwidth=4
set expandtab

" Line Numbers
set number
